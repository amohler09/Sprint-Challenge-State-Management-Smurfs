const initialState = {
    isLoading: false,
    smurfReceived: {},
    error: '',
    smurfSent: {}
};

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}