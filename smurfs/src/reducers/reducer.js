const initialState = {
    smurfs: [],
    isFetching: false,
    isSending: false
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }

}